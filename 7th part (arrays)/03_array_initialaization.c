#include <stdio.h>

int main()
{
    int a[] = {1, 2, 3};
    printf("valuse of a[0] is %d. \n", a[0]);
    printf("valuse of a[1] is %d. \n", a[1]);
    printf("valuse of a[2] is %d. \n", a[2]);
    float b[] = {1, 2, 3};
    printf("valuse of b[0] is %f. \n", b[0]);
    printf("valuse of b[1] is %f. \n", b[1]);
    printf("valuse of b[2] is %f. \n", b[2]);
    return 0;
}
